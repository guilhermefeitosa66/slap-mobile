Um arquivo só para baixar.

## Mudou

- **Um APK, em vez de três.** As versões anteriores publicavam `arm64-v8a`, `armeabi-v7a` e
  `x86_64`, e quem ia instalar não tinha como saber qual era o do celular dele — escolher errado dá
  "aplicativo não instalado", sem dizer por quê. Agora é **`slap-1.2.1.apk`**, e ele serve a
  qualquer celular Android.

  O arquivo ficou maior, cerca de 50 MB, porque carrega dentro as duas arquiteturas ARM. É o preço
  de não fazer ninguém escolher, e quem baixa o faz uma vez por versão.

  Emuladores e Chromebooks com processador Intel ou AMD não são atendidos: não fazem inventário em
  campo.

Nada mais mudou em relação à [1.2.0](https://github.com/guilhermefeitosa66/slap-mobile/releases/tag/v1.2.0),
que trouxe a importação da planilha do `inventario.ifpi.edu.br` e o aviso de versão nova.

## Atualizar

Instale o APK **por cima** da versão anterior: os inventários ficam. **Não desinstale** para
atualizar — desinstalar apaga o que ainda não foi trocado com outro aparelho.

Quem nunca instalou: baixe `slap-1.2.1.apk` e siga o
[passo a passo de instalação](https://github.com/guilhermefeitosa66/slap-mobile/blob/main/docs/instalacao.md).

Para conferir que o arquivo é este: a soma SHA-256 está em `SHA256SUMS.txt`, e o certificado de
assinatura tem a impressão digital SHA-256

```
{{certificado}}
```

a mesma de todas as versões.

## O que ainda não faz

- **Não atualiza o SUAP.** Gera as planilhas para quem faz a atualização.
- **Não lê `.xls`** (Excel 97-2003). Abra a planilha e salve como `.xlsx` ou `.csv`.
- **Não sincroniza pela internet** — só na mesma rede local, por projeto. Redes que isolam os
  aparelhos entre si não funcionam; nesse caso, um dos celulares compartilha a conexão (ponto de
  acesso) e os outros se conectam nele.
- **Só Android**, 7.0 ou mais novo, com processador ARM. Ainda não está na Play Store nem há
  versão para iPhone.
- **Em tablet**, a interface é a mesma do celular, mais larga.

## Privacidade

Sem conta, sem servidor, sem anúncios. Detalhes na
[política de privacidade](https://guilhermefeitosa66.github.io/slap-mobile/privacidade/).
